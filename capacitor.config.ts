import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lanthanum89.lifedashboard',
  appName: '429: Too Many Tabs',
  webDir: 'dist'
};

export default config;
